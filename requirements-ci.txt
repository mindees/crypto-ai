# Minimal dependencies for GitHub Actions daily/weekly jobs.
# Deliberately EXCLUDES TensorFlow, matplotlib, ccxt, yfinance — CI never
# trains and only needs ingestion + validation + drift (pure numpy/pandas).
numpy==2.1.3
pandas==2.2.3
pyarrow==18.1.0
PyYAML==6.0.2
requests==2.32.3
